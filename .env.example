# The prompted-judge experiments (exp2, exp4, exp5, exp5b, exp7) read this key.
# Everything else in the pipeline runs without it; the judge is skipped with a
# printed notice when it is absent. See the README for caching and the spend cap.
DEEPSEEK_API_KEY=sk-your-key-here
